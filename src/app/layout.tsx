import type { Metadata } from "next";
import { Inter, Playfair_Display } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

const playfair = Playfair_Display({
  variable: "--font-playfair",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dunham Road Unitarian Church | Altrincham",
  description: "Welcome to Dunham Road Unitarian Church in Altrincham. A welcoming community celebrating diversity, spiritual growth, and social justice.",
  keywords: "Unitarian Church, Altrincham, Dunham Road, spiritual community, inclusive worship",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${inter.variable} ${playfair.variable} font-sans antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
